# Signal / Noise — Fallacy Lab

A local-first React app for learning to spot logical fallacies in real arguments.

## Run locally

Run npm install, then npm run dev.

The Vite server runs on http://localhost:3000.

Progress is stored in the browser with localStorage. There is no backend or account layer.
