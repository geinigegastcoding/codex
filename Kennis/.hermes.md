# Wiki Repository Rules

This repository is Daniel's Obsidian-compatible second brain.

- Read [[00-authority]] before modifying context.
- Keep Magisdata-only notes under `company/`; keep personal context under `personal/`.
- Preserve `logs/` and never delete information; archive superseded notes.
- Use Obsidian wikilinks such as `[[00-authority]]` and dated YAML frontmatter for new durable notes.
- Update navigation and logs when adding important notes.
- Run the maintenance contract before declaring structural cleanup complete.
